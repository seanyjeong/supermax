const express = require('express');
const router = express.Router();
const { verifyToken } = require('../middleware/auth');

// TODO: Implement salary management routes
module.exports = router;
